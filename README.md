AEP

Breve descrição do que a aplicação faz e qual o objetivo.

## 🚀 Tecnologias
- React / TypeScript / Vite
- Axios (Consumo de API)
- Tailwind CSS (Estilização)

## 📦 Como rodar o projeto
1. Clone o repositório: `git clone https://github.com/usuario/projeto.git`
2. Instale as dependências: `npm install`
3. Crie um arquivo `.env` com a URL da API (veja o exemplo abaixo)
4. Inicie o servidor: `npm run dev`

Integrantes:

-Guilherme da Costa Castro -Pedro Magalhães -Gabriel Vinicius Silva Marchetto 
